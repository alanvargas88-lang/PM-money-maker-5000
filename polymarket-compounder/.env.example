# ============================================================
# Polymarket Compounder - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit your .env file to version control.
# ============================================================

# ---- Wallet ----
# Your Polygon wallet private key (with 0x prefix).
# This wallet must hold USDC on Polygon for trading.
PRIVATE_KEY=0x_your_private_key_here

# ---- Polymarket API Credentials ----
# If left blank, the bot derives these from your private key on first run
# using the Polymarket CLOB client's derive_api_key() method.
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_PASSPHRASE=

# ---- Optional: Telegram Alerts ----
# Create a bot via @BotFather, get the token.
# Send a message to @userinfobot to get your chat ID.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ---- Mode ----
# DRY_RUN=true (default): Simulates trades, no real orders placed.
# Set to false ONLY when you are ready for live trading.
DRY_RUN=true

# ACTIVE_PHASE controls which strategies run:
#   0 = Auto-phase based on balance (recommended)
#   1 = Phase 1 only (resolution arb + sum-to-one arb)
#   2 = Phase 1 + 2 (adds new market sniping)
#   3 = Phase 1 + 2 + 3 (adds directional engine)
ACTIVE_PHASE=0
